spring:
  application:
    name: web
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    config: 
      # uri: http://localhost:8888/
      label: master
      profile: dev
      discovery:
        enable: true
        serviceId: configserver